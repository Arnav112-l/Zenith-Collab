# Shared Prisma schema (source of truth)

Edit `schema.prisma` here, then sync into client/server:

```bash
node scripts/sync-prisma.mjs
```

Or run `npm run prisma:generate` from `client/` or `server/` (syncs automatically).
